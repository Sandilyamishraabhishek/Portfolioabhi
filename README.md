
# Interactive Landing - React (Vite)

## Quick start
1. Ensure Node.js & npm are installed.
2. In the project folder:
   npm install
   npm run dev
3. Build for production:
   npm run build

This project uses Vite. The `package.json` included defines dev/build scripts.
